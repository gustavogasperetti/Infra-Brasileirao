# Brasileirão Analytics — Dependências do Projeto
# Gerado para Python 3.10+

# --- Web Scraping (Camada Bronze) ---
playwright>=1.44.0
beautifulsoup4>=4.12.0
lxml>=5.2.0          # Parser HTML/XML de alta performance para BeautifulSoup

# --- Processamento de Dados ---
pandas>=2.2.0
numpy>=1.26.0

# --- Google Sheets (Camada Load) ---
gspread>=6.1.0            # Cliente da API do Google Sheets
google-auth>=2.29.0       # Autenticação via Service Account

# --- Utilitários ---
python-dotenv>=1.0.0     # Leitura do .env (desenvolvimento local)

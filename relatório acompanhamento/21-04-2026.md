# 📋 Relatório de Acompanhamento — 21/04/2026

**Projeto:** Brasileirão Analytics — Infraestrutura e Engenharia de Dados  
**Repositório:** `brasileirao-infra` (Polyrepo)  
**Responsável:** Gustavo Leite Gasperetti  
**Objetivo geral:** TCC de Ciência da Computação focado em Engenharia de Dados

---

## ✅ O que foi feito até esta data

### 1. Definição da Arquitetura do Projeto

- Adotada arquitetura **Polyrepo**, separando o back-end/dados (`brasileirao-infra`) da biblioteca cliente (`brasileirao-lib`, repositório futuro).
- Definida a **Medallion Architecture** (Bronze → Prata → Ouro) como pipeline de dados.
- Modelagem do banco de dados como **Star Schema** (OLAP), com tabelas `dim_times` e `fato_partidas_ouro`.

### 2. Estrutura de Diretórios

O repositório foi organizado com a seguinte estrutura base:

```
brasileirao-infra/
├── .env                    # Variáveis de ambiente (DB_USER, DB_PASS, API_PORT)
├── .gitignore              # Ignora /data, /venv e __pycache__
├── docker-compose.yml      # Imagem do PostgreSQL
├── requirements.txt        # Dependências do projeto
├── README.md               # Documentação principal
│
├── data/                   # Armazenamento local (ignorado no Git)
│   ├── bronze/
│   ├── silver/
│   └── gold/
│
├── etl/                    # Scripts de Engenharia de Dados
│   ├── __init__.py
│   ├── config.py
│   ├── extract.py
│   ├── transform.py
│   └── load.py
│
└── api/                    # Aplicação FastAPI
    ├── __init__.py
    ├── main.py
    ├── database.py
    ├── models.py
    └── routes.py
```

### 3. Stack Tecnológico Definido e Configurado

| Componente | Tecnologia |
|---|---|
| Linguagem principal | Python 3.x |
| Processamento de dados | Pandas, NumPy |
| Banco de dados | PostgreSQL (via Docker) |
| ORM e ingestão | SQLAlchemy |
| API REST | FastAPI + Uvicorn |
| Web Scraping | BeautifulSoup4 + **Playwright** |
| Parser HTML | lxml |
| Configuração de ambiente | python-dotenv |

> **Nota:** A dependência de `requests` foi mantida no `requirements.txt`, mas o scraping foi implementado com **Playwright** (headless Chromium), que permite autenticação real no site ogol.com.br.

### 4. `etl/config.py` — Configuração Central

- Criado o arquivo `config.py` com as **55 URLs mapeadas** para cada edição do Brasileirão de **1971 a 2025**, apontando para o site ogol.com.br.
- Configurada a lista `OGOL_ACCOUNTS` com suporte a **múltiplas contas** (sistema de fallback automático para quando o limite de visualizações é atingido).

### 5. `etl/extract.py` — Camada Bronze (Web Scraping) — v5.0.0

Este foi o principal script desenvolvido na sessão de 21/04. É responsável por toda a extração de dados brutos.

**Funcionalidades implementadas:**

- **Autenticação via Playwright:** Login real no ogol.com.br usando Chromium headless, com suporte a XPath do formulário de login.
- **Sistema de troca automática de contas (`login_with_fallback`):** Ao atingir o limite de visualizações, o script faz logout da conta atual e tenta login com a próxima conta da lista, retomando do mesmo ano e página onde parou.
- **Detecção de limite de visualizações (`is_view_limit_reached`):** Verifica frases-chave no HTML (PT/EN) para detectar o bloqueio do site.
- **Paginação automática (`scrape_year`):** Itera por todas as páginas de um calendário anual, com:
  - Controle de `MAX_EMPTY_STREAK` (3 páginas vazias consecutivas encerram o loop).
  - Rate limiting com `time.sleep()` entre páginas (1.5s) e entre anos (2.0s).
  - Tratamento de `TimeoutError` do Playwright.
- **Parsing de HTML (`parse_row`):** Extração dos dados de cada jogo via BeautifulSoup4, capturando:
  - `Data`, `Mandante`, `Placar_Bruto`, `Visitante`, `Fase`.
- **Persistência (`save_to_csv`):** Salva os jogos de cada ano em `data/bronze/jogos_{ano}.csv`.
- **Logging estruturado:** Uso do módulo `logging` com timestamps, níveis INFO/WARNING/ERROR e emojis para fácil leitura do progresso.
- **Resumo final:** Exibe ao final da execução a lista de anos com sucesso, anos com falha e total de jogos extraídos.

### 6. Esqueleto da API e demais módulos ETL

Os seguintes arquivos foram criados como esqueleto/estrutura inicial, aguardando implementação:

- `api/main.py` — Ponto de entrada do FastAPI
- `api/database.py` — Configuração da Session e Engine do SQLAlchemy
- `api/models.py` — Classes declarativas (Base) das tabelas
- `api/routes.py` — Endpoints GET para consulta dos dados
- `etl/transform.py` — Lógica Bronze → Prata → Ouro (a implementar)
- `etl/load.py` — Ingestão no PostgreSQL via SQLAlchemy (a implementar)

---

## 🔜 Próximos Passos

- [ ] Executar `extract.py` e validar os CSVs gerados na camada Bronze.
- [ ] Implementar `etl/transform.py` (Camada Prata: limpeza, tipagem, padronização de nomes de clubes).
- [ ] Implementar a Camada Ouro: Feature Engineering (médias móveis, win streaks, pts_padronizados, saldo acumulado).
- [ ] Implementar `etl/load.py` para ingestão no PostgreSQL via SQLAlchemy.
- [ ] Implementar os endpoints da API FastAPI em `api/routes.py`.
- [ ] Subir o banco de dados PostgreSQL via `docker-compose.yml` e testar a conexão.

---

## 📝 Observações e Decisões Técnicas

- O campo `Placar_Bruto` é salvo como string na camada Bronze (ex: `"2 - 1"`). O parse numérico (gols_mandante, gols_visitante) será feito na camada **Prata**.
- A coluna `Fase` é fundamental para suportar os formatos históricos variados do campeonato (Quadrangular Final, Repescagem, etc.).
- O diretório `data/` é ignorado pelo Git (`.gitignore`), pois os dados brutos podem ser regenerados via scraping.
